import { Express } from 'express-serve-static-core';
export default class MainModule {
    private app;
    private branch;
    private user;
    private wallet;
    private barkota;
    private admin;
    constructor(app: Express);
}
